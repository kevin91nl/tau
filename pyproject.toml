[project]
name = "tau"
version = "0.1.0"
description = "Tau: Pi-plugin-first local self-improvement harness."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
tau = "tau_cli.main:main"

[tool.setuptools]
packages = ["tau_core", "tau_cli"]
